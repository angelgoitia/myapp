<div class="col-md10 mx-auto text-center">
    <h3>Listado de Profesores</h3>
<table class="table table-hover table-dark">
        <thead>
            <tr>
                <th scope="col">Nombre</th>
                <th scope="col">Carrera</th>
                <th scope="col">Materia</th>
                <% if current_user.role == "admin" %>
                <th scope="col">Opción</th>
                <%end%>
            </tr>
        </thead>
        <tbody>
            <% if @teachers %>
                <% @teachers.each do |teacher| %>
                    <tr>
                        <td><%= teacher.name %></td>
                        <td><%= teacher.career %></td>
                        <td><%= teacher.subject %></td>
                        <% if current_user.role == "admin" %>
                        <td>
                            <%= link_to "Editar", '/admin/profesor/'+teacher.id.to_s+'/edit', class: "btn btn-outline-warning" %>
                            <%= link_to "Eliminar", '/admin/profesor/'+teacher.id.to_s, class: "btn btn-outline-danger" %>
                        </td>
                        <%else%>
                        <td>
                           <%= link_to "Seleccionar", '/users/profesor/'+teacher.id.to_s+'/select', class: "btn btn-outline-success" %>
                        </td>
                        <% end %>
                    </tr>
                <% end %>
            <%else%>
                <tr class="bg-danger">
                    <td colspan="4" class="text-center">No hay Profesor</td>
                </tr>
            <% end %>
        </tbody>
    </table>
</div>